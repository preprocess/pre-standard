{
    "private": true,
    "scripts": {
        "format": "prettier --config=.prettierrc --write --parser=php"
    },
    "devDependencies": {
        "@prettier/plugin-php": "^0.9.0",
        "atob": "^2.1.2",
        "prettier": "^1.15.3"
    }
}
